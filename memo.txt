Global setup:
 Set up git http://help.github.com/set-up-git-redirect
  git config --global user.name "Yukio MURAKAMI"
  git config --global user.email m_yukio@mac.com
      
Next steps:
  mkdir workbook
  cd workbook
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:murakami/workbook.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:murakami/workbook.git
  git push -u origin master
      
Importing a Subversion Repo?
  Click here https://github.com/murakami/workbook/imports/new
      
When you're done:
  Continue https://github.com/murakami/workbook
